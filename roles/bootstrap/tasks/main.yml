- name: Install Python on Alpine
  ansible.builtin.raw: apk add --no-cache python3 py3-pip
  changed_when: false  # Le decimos que asuma que no cuenta como cambio de estado critico
  when: ansible_os_family == 'Alpine'