---
- name: Base Server Configuration
  hosts: all
  become: true
  roles:
    - bootstrap
    - common
    - security

- name: Docker Host Configuration
  hosts: all
  become: true
  roles:
    - docker
